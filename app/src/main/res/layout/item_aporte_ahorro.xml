<?xml version="1.0" encoding="utf-8"?>
<!-- Una fila de aporte a un plan: selector de plan + monto + botón quitar.
     El Fragment crea varias de estas dentro del contenedor de ahorro. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="8dp">

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginBottom="8dp"
        android:background="?attr/colorOutlineVariant" />

    <!-- Fila: spinner del plan + botón quitar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <Spinner
            android:id="@+id/spinnerPlan"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1" />

        <ImageView
            android:id="@+id/btnQuitar"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:contentDescription="Quitar plan"
            android:layout_marginStart="8dp" />
    </LinearLayout>

    <!-- Monto a ahorrar en este plan -->
    <EditText
        android:id="@+id/inputMonto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Monto a ahorrar"
        android:textColorHint="@color/gris_texto"
        android:inputType="numberDecimal" />

    <!-- Info: cuánto falta para la meta de este plan -->
    <TextView
        android:id="@+id/txtInfo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="12sp"
        android:textColor="@color/gris_texto"
        android:layout_marginTop="2dp" />

</LinearLayout>
