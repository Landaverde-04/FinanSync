<?xml version="1.0" encoding="utf-8"?>
<!-- Fondo blanco con esquinas redondeadas (reemplaza al CardView sin librería extra) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/color_tarjeta" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="@color/colorOutlineVariant" />
</shape>
